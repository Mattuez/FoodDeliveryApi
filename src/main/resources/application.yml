spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/fooddeliveryapi?createDatabaseIfNotExist=true&serverTimeZone=UTC
    username: root
    password: M_th__s03!
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 10
      idle-timeout: 10000
  jackson:
    deserialization:
      fail-on-unknown-properties: true
      fail-on-ignored-properties: true
  mvc:
    throw-exception-if-no-handler-found: true
  jpa:
    properties:
      #javax:
        #persistence:
          #schema-generation:
            #scripts:
              #action: create
              #create-target: src/main/resources/ddl.sql
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
  flyway:
    baseline-on-migrate: true
    locations: classpath:db/migration, classpath:db/testdata
  web:
    resources:
      add-mappings: false
  servlet:
    multipart:
      max-file-size: 1MB
      max-request-size: 20MB
  mail:
    host: smtp.sendgrid.net
    port: 587
    username: apikey
    password:

foodDeliveryApi:
  storage:
    local:
      pictures-directory: /Users/mathe_0dd5ya5/Documents/catalogo
  email:
    sender: FoodDelivery <fooddeliveryapiteste@gmail.com>
    impl: sand_box